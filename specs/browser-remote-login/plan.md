# 계획

## Phase 0 — 선행 (이 기능보다 먼저)

포트 소유권 검증 공백을 닫는다. spec.md 의 "질문 1 답변" 참조.
**이 단계 전에는 아래를 구현하지 않는다** — 구현하면 로그인된 브라우저를
공유 머신에 띄우도록 권하면서 그 접근 통제는 비워 두는 셈이 된다.

- [ ] `worktreeId` 없는 발급 경로에도 포트 소유권 검증 적용
      (또는 CDP 계열 포트를 릴레이 대상에서 제외)
- [ ] 회귀 테스트: 임의 포트 요청이 거부되는지

## Phase 1 — 조사 마무리

- [ ] 릴레이 토큰 만료값이 로그인 소요 시간에 충분한지 확인
- [ ] DevTools 프론트엔드가 릴레이 뒤에서 뜨는지 실측
      (`rewriteHtml` shim 이 절대 URL 을 처리하는가)

## Phase 2 — 구현

- [ ] CDP 전용 토큰 종류 분리 (AC3) — dev server 토큰이 브라우저 통제로
      승격되지 않게
- [ ] 머신 화면에 "브라우저 열기" 버튼 + 위험도 명시 (AC4)

## Phase 3 — 검증

- [ ] 컨테이너에서 릴레이 경유 DevTools 접속 실측
- [ ] 인증 없는 접근이 거부되는지 (AC2)
